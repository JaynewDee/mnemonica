import { Dispatch, SetStateAction } from "react";

interface MenuProps {
  title: string;
  subtitle: string;
}
interface TitleProps {
  title: string;
  subtitle: string;
}
export type { MenuProps, TitleProps };
